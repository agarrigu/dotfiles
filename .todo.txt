 _______  _______  _______  _______
|_     _||   _   ||  _    ||   _   |
  |   |  |  | |  || | |   ||  | |  |
  |   |  |  |_|  || |_|   ||  |_|  |
  |   |  |       ||       ||       |
  |___|  |_______||______| |_______|

######################
# THINGS TO AUTOMATE #
######################

# INSTALL APPS #

# Essentials with apt:

git
tmux
cmake
tree
thefuck
xclip or oher clipboard
curl
wget
zsh
python3
python3-pip
npm
fzf
nodejs > 12
lua5.x > 5

# neovim dependencies

python -m install --user --ugrade pynvim
npm install -g neovim
fdfind
ripgrep

# Essentials from release

neovim

# Fancy from apt:

lolcat
figlet
cowsay
sl
font-manager

# CONFIGURATIONS #

set shh to github:alonsogmunoz
    create ssh key (add to github)
    $ ssh-keygen -t ed25519 -C "your_email@example.com"
    add to agent
    $ eval "$(ssh-agent -s)" //Starts agent in background
    $ ssh-add ~/.ssh/id_ed25519 //adds to agent from default location
set zsh as deafult shell (oh-my-zsh does it automatically?)
pull dotfiles on logon -- maybe not a great idea. would go in .zshlogin
push dotfiles on logout/shutdown/sleep/reboot -- in .zshlogout

####################################
# THINGS TO UNDERSTAND AND INSTALL #
####################################

nvim debugger

########################
# CONFIGURATION TO ADD #
########################

Treesitter indent when stable
Make single .lua with languages for lspconfig.lua and lspinstall.lua

######################
# THINGS TO WAIT FOR #
######################

 -- This section is empty at the moment -- You have nothing to look forward to.
