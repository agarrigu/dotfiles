 _______  _______  _______  _______
|_     _||   _   ||  _    ||   _   |
  |   |  |  | |  || | |   ||  | |  |
  |   |  |  |_|  || |_|   ||  |_|  |
  |   |  |       ||       ||       |
  |___|  |_______||______| |_______|

######################
# THINGS TO AUTOMATE #
######################

# INSTALL APPS #

# Essentials with apt:

git
cmake
tree
thefuck
xclip or oher clipboard
curl
wget
zsh
python3
python3-pip
npm
fzf
nodejs > 12
lua5.x > 5

# neovim dependencies

python -m install --user --ugrade pynvim
npm install -g neovim
fdfind
ripgrep

# Essentials from release

neovim
zellij

# Tools from curl or wget

oh-my-zsh
    sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
powerover10k
    git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k

# Fancy from apt:

lolcat
figlet
cowsay
sl
xfce4-eyes-plugin
font-manager

# Fancy from release

ProggyClean TTCC Nerd Font (get curl)
    curl -fL https://github.com/ryanoasis/nerd-fonts/blob/master/patched-fonts/ProggyClean/Regular/complete/ProggyCleanTT%20Nerd%20Font%20Complete%20Mono.ttf -o $HOME/Downloads/ProggyCleanTT.ttf | font-manager -i
GNU unifont
    curl -fL http://unifoundry.com/pub/unifont/unifont-14.0.01/font-builds/unifont-14.0.01.ttf -o Downloads/unifont.ttf | font-manager -i
# CONFIGURATIONS #

source .profile after login
set shh to github:alonsogmunoz
    create ssh key (add to github)
    $ ssh-keygen -t ed25519 -C "your_email@example.com"
    add to agent
    $ eval "$(ssh-agent -s)" //Starts agent in background
    $ ssh-add ~/.ssh/id_ed25519 //adds to agent from default location
set zsh as deafult shell (oh-my-zsh does it automatically?)
pull dotfiles on logon
push dotfiles on logout/shutdown/sleep/reboot

####################################
# THINGS TO UNDERSTAND AND INSTALL #
####################################

nvim debugger

########################
# CONFIGURATION TO ADD #
########################

Treesitter indent when stable

######################
# THINGS TO WAIT FOR #
######################

advent-of-code 2021
Neovim 0.6 stable with Treesitter December 25
